<div class="show-container">
  <div class="row">
    <div class="col-sm-12 col-lg-4">
      <%= render 'shared/seller-profile' %>
    </div>
    <div class="col-sm-12 col-lg-8">
      <div class="border border-light bg-light">
        <p class="h3 text-center p-3">My Gigs</p>
      </div>
        <% if current_user.id == @seller.id && current_user.sellers? %>
          <div class="text-center py-5">
            <p class="h4 text-secondary py-3">Let's Create One Now</p>
            <%= link_to 'Create Gig', new_service_path, class: 'btn btn-primary' %>
          </div>
        <% end %>
        <% if @services.present? %>
          <div class="row">
            <%= render partial: 'shared/services-row', locals: {services: @services} %>
          </div>
        <% end %>
        <%# end %>
      <div class="border border-light bg-light">
        <p class="h3 text-center p-3">My Favorites</p>
      </div>
      <div class="row">
        <%= render partial: 'shared/services-row', locals: {services: @favorited_services} %>
      </div>
    </div>
  </div>
</div>

